# Local tooling
.gstack/
.DS_Store
.vscode/
.idea/

# Claude Code worktree-isolated subagents (per ce-work skill)
.claude/worktrees/

# Secrets
.env
.env.*
!.env.example

# Node / Vite / TS unified (D-012, Phase 1+)
node_modules/
dist/
build/
.vite/
.turbo/
.next/
*.log
*.tsbuildinfo
coverage/

# Sui Move (contracts/, Phase 2+)
contracts/**/build/

# Throwaway sketches (kept outside repo, never commit by accident)
/tmp/box-demo/

# Quota store (node:sqlite) — runtime durable state, never committed (D-083, U1)
backend/data/
*.db
*.db-journal
*.db-wal
