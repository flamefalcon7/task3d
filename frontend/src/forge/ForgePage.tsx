// Stub — overwritten by U4 (plan-003).
// Existence at this path lets App.tsx wire the /forge route without colliding
// with U4's full implementation.
export function ForgePage() {
  return <div>Collection Forge — coming in U4.</div>;
}
