// Stub — overwritten by U5 (plan-003).
// Existence at this path lets App.tsx wire the /collection/:slug route
// without colliding with U5's full implementation.
export function CollectionDetailPage() {
  return <div>Collection detail — coming in U5.</div>;
}
