// Canonical embedded tusk model, served from frontend/public. One source for the
// landing hero and every live lifecycle panel (U1, plan
// 2026-06-06-001-feat-landing-live-3d-wells). Replaces the older 667KB
// walrus-tusk.glb; this 345KB mesh is cleaner and lighter for multi-scene use.
export const EMBEDDED_TUSK_GLB_URL = '/models/tusk3d/tusk.glb';

// The "monster" model used in the IN-GAME lifecycle panel vignette.
export const MONSTER_GLB_URL = '/models/tusk3d/monster.glb';
